# 3D 自动立体车库仿真系统

基于 Three.js 的升降横移类立体车库实时仿真，包含多层停车架、升降机、每层横移台车与车辆模型。

## 运行

```bash
python3 -m http.server 8000
# 打开 http://localhost:8000
```

## 功能

- 3 层 × 5 列停车架，升降机（带转盘）+ 每层横移台车联动
- 入库自动分配空闲车位，取车执行反向流程，支持任务排队
- 所有运动由运动学状态机实时计算（速度积分），非固定动画
- 实时显示设备状态、车位占用率、当前任务与运行日志
- 支持点击 3D 车位直接取车、仿真速度调节
